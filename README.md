# check-scaffold-mito
Identify which genes in the reference genome (FASTA format) assembled into the scaffold are mitochondrial genes, thus preparing for the quality control steps of the Cellranger process.

Note: To run the bash scripts in .md file, you need to download the Markdown Execute plugin.